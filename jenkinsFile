pipeline {
    agent any

    stages {
        stage('validate') {
            steps {
                echo 'validating'
            }
            }
            stage('compile'){
            steps{
            echo 'compiling'
            }
            }
    }
}
